exhaustion at ~7200 MB
int main()
try {
	int i = 0;
	while (true) {
		std::cout << ++i << " MB\n";
		char* pd = new char[1048576];    
		}


///////////////////////////////////////////////////////
overflow at 3192 MB
void mem_over(int size) {
	char* p = new char[1048576];
	++size;
	std::cout << size << "MB\n" << "\n";
	mem_over(size);
	//std::cout << sizeof(*p) << "\n";
}

mem_over(0);

